import { NgModule } from '@angular/core';

import * as clients from './clients';

const _CLIENTS = [
{% for c in Model -%}
    clients.{{ c }},
{% endfor -%}
];

@NgModule({
    providers: [..._CLIENTS],
})

export class AbpServiceModule { }
